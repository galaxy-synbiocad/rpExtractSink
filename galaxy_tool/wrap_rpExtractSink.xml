<tool id="rpExtractSink" name="Sink from SBML" version="1.0">
  <description>Generate the sink from an SBML file</description>
  <requirements>
		<container type="docker">brsynth/rpextractsink-standalone:dev</container>
  </requirements>
  <command detect_errors="exit_code"><![CDATA[
		'$__tool_directory__/tool_rpExtractSink.py' -input_sbml '$input_sbml' -output_sink '$output_sink' -compartment_id '$adv.compartment_id'
		]]></command>
  <inputs>
    <param name="input_sbml" type="data" format="sbml" label="SBML" optional="false" />
		<section name="adv" title="Advanced Options" expanded="false">
			<param name="compartment_id" type="text" value="MNXC3" label="Sink Compartment" />
		</section>
  </inputs>
  <outputs>
    <data name="output_sink" format="csv" label="Sink Compounds" />
  </outputs>
  <help>
    Extract the sink from a SBML file from a particular compartment. The tool uses MetaNetX identifiers from the MIRIAM annotations from the SBML and finds the InChI structure from preparsed MetaNetX data.
  </help>
</tool>
